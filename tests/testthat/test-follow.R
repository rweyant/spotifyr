context("Follow endpoints")

test_that("follow functions return expected content", {

  expected_album_fields <- c()

  # setup
  set_tokens()
  # user_auth() # need to figure out a way to do this with cached

  # response <- get_user_followed_artists()
  # expect_is(typeof(response), 'character')

})

